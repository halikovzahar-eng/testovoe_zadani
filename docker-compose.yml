version: '3.8'

services:
  backend:
    build: ./backend
    container_name: effective-backend
    restart: unless-stopped
    expose:
      - "8080"
    networks:
      - app-network

  nginx:
    image: nginx:1.26-alpine
    container_name: effective-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
    - ./nginx/nginx.conf/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
    name: effective-network